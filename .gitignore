# Dependencies
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
package-lock.json
yarn.lock

# Build output
build/
dist/
lib/

# Test coverage
coverage/

# Environment variables
.env*
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Tokens / encrypted session cache (contains sensitive authentication data).
# The encrypted cache normally lives outside the repo entirely (per-user app data dir,
# see src/msal-client.ts) — these are defensive in case MSTODO_TOKEN_FILE is ever
# pointed at a path inside the repo.
tokens.json
token-cache.bin
.cloud-token-cache.bin

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

